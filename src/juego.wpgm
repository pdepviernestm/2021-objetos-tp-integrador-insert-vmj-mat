import wollok.game.*
import menu.*
import pantallaInicio.*
import mapa.*
import elementos.*

program juego {
	
	game.cellSize(32)
	game.height(15)
	game.width(20)
	
	
	
	pantallaInicio.iniciar()
	game.boardGround("background/inicio.jpeg")
	keyboard.down().onPressDo{ modo.puntero().moverseHacia(abajo) }
	keyboard.up().onPressDo{ modo.puntero().moverseHacia(arriba) }
	keyboard.x().onPressDo{ modo.puntero().seleccionar() }
	keyboard.left().onPressDo{ modo.puntero().moverseHacia(izquierda) }
	keyboard.right().onPressDo{ modo.puntero().moverseHacia(derecha) }

	game.start()

}
